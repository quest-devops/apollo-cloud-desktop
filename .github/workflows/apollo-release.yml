# SPDX-License-Identifier: GPL-2.0-or-later
# Apollo Cloud — compila e EMPACOTA o instalador Windows do cliente branded.
# Diferente do windows-build-and-test.yml oficial (que só compila+testa em PR),
# este roda sob demanda (workflow_dispatch) e no push da branch apollo-brand,
# gera o instalador via Craft (NullsoftInstallerPackager) e publica como artefato.
name: Apollo Cloud - Windows Installer
on:
  workflow_dispatch:
  push:
    branches: [apollo-brand]
jobs:
  build:
    name: Build & Package (Windows)
    # Manter windows-2022: o Craft (vswhere embutido) NAO detecta o VS na imagem
    # windows-2025 (testado 12/ago/2026, run 31593606310). A imagem 2022 atual tem
    # um certificado malformado no store que derruba o python do Craft (openssl
    # 3.6.3, ASN1 estrito) na SONDAGEM de import do truststore do pip — o shim
    # sitecustomize abaixo contorna. Upstream nao sente: o cache do Craft deles
    # pula o pip. Historico completo na E5 do ApolloPlan.
    runs-on: windows-2022
    env:
      CRAFT_TARGET: windows-msvc2022_64-cl
      CRAFT_MASTER_LOCATION: ${{ github.workspace }}\CraftMaster
      CRAFT_MASTER_CONFIG: ${{ github.workspace }}\craftmaster.ini
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Shim p/ cert malformado no store da imagem (PYTHONPATH sitecustomize)
        shell: pwsh
        run: |
          # A imagem windows-2022 (~ago/2026) tem um certificado que o openssl 3.6.3
          # do python interno do Craft rejeita (ASN1: NOT_ENOUGH_DATA) na sondagem
          # de import do truststore do pip. O shim engole SO esse erro de carga em
          # bloco do store; a validacao HTTPS real do truststore usa a CryptoAPI
          # por conexao e nao passa por aqui — nada de confianca e enfraquecido.
          $dir = "${{ github.workspace }}\.ci-shim"
          New-Item -ItemType Directory -Force $dir | Out-Null
          @'
          import ssl as _apollo_ssl
          _apollo_orig = _apollo_ssl.SSLContext._load_windows_store_certs
          def _apollo_safe(self, storename, purpose):
              try:
                  return _apollo_orig(self, storename, purpose)
              except _apollo_ssl.SSLError:
                  import sys
                  print("[apollo-shim] store %s tem cert malformado; pulando carga em bloco" % storename, file=sys.stderr)
          _apollo_ssl.SSLContext._load_windows_store_certs = _apollo_safe
          '@ -replace '(?m)^          ', '' | Set-Content -Encoding utf8 "$dir\sitecustomize.py"
          python -c "import sys; sys.path.insert(0, r'$dir'); import sitecustomize; print('[apollo] shim importa ok')"
          Add-Content $env:GITHUB_ENV "PYTHONPATH=$dir"

      - name: Install Craft + client deps
        shell: pwsh
        run: |
          & cmd /C "git clone -q --depth=1 https://invent.kde.org/packaging/craftmaster.git ${{ env.CRAFT_MASTER_LOCATION }} 2>&1"
          function craft() {
              python "${{ env.CRAFT_MASTER_LOCATION }}\CraftMaster.py" --config "${{ env.CRAFT_MASTER_CONFIG }}" --target ${{ env.CRAFT_TARGET }} -c $args
              if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
          craft --add-blueprint-repository "https://github.com/nextcloud/craft-blueprints-kde.git|stable-34.0|"
          craft --add-blueprint-repository "https://github.com/nextcloud/craft-blueprints-nextcloud.git|stable-34.0|"
          craft craft
          craft --install-deps nextcloud-client

      - name: Install Inkscape (icon rasterization)
        shell: pwsh
        run: choco install inkscape -y --no-progress

      - name: Compile
        shell: pwsh
        run: |
          function craft() {
              python "${{ env.CRAFT_MASTER_LOCATION }}\CraftMaster.py" --config "${{ env.CRAFT_MASTER_CONFIG }}" --target ${{ env.CRAFT_TARGET }} -c $args
              if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
          craft --src-dir ${{ github.workspace }} nextcloud-client

      - name: Patch upstream blueprint (import os + marca Apollo)
        shell: pwsh
        run: |
          # (1) Bug upstream em craft-blueprints-nextcloud@stable-34.0: nextcloud-client.py
          #     usa os.path.join em createPackage() sem 'import os' -> NameError.
          # (2) appname/company do blueprint ainda sao Nextcloud -> Apollo.
          $bp = "${{ github.workspace }}\${{ env.CRAFT_TARGET }}\etc\blueprints\locations\craft-blueprints-nextcloud\nextcloud-client\nextcloud-client.py"
          if (-not (Test-Path $bp)) { Write-Host "[apollo] AVISO: blueprint nao encontrado em $bp"; exit 1 }
          $c = Get-Content $bp -Raw
          if ($c -notmatch '(?m)^import os\b') { $c = "import os`r`n" + $c; Write-Host "[apollo] +import os" }
          $c = $c -replace '(?m)(self\.defines\["company"\]\s*=\s*)"Nextcloud GmbH"', '$1"Apollo Solution"'
          # appname + icone do instalador (o packager NSIS le self.defines["icon"] -> MUI_ICON;
          # o admin/win/nsi/installer.ico do repo e ignorado pelo template do Craft).
          $icon = "${{ github.workspace }}\admin\win\nsi\installer.ico"
          $appLine  = '        self.defines["appname"] = "apollocloud"'
          $iconLine = '        self.defines["icon"] = r"' + $icon + '"'
          # productname controla o nome exibido no instalador, a pasta em Program Files
          # e o DisplayName no "Programas e Recursos" -> sem ele, tudo fica "Nextcloud".
          $prodLine = '        self.defines["productname"] = "ApolloCloud"'
          $c = $c -replace '(?m)^[ \t]*self\.defines\["appname"\]\s*=\s*"nextcloud".*$', ($appLine + "`r`n" + $iconLine + "`r`n" + $prodLine)
          Set-Content $bp $c -Encoding utf8 -NoNewline
          Write-Host "[apollo] blueprint patchado (import os, appname, company, productname=ApolloCloud, icon=$icon)"

          # blacklist.txt do Craft remove 'bin/*.exe' cujo nome != nextcloud/nextcloudcmd.
          # Com o rename p/ apollocloud.exe, isso APAGAVA o executavel do app do pacote
          # (sobravam so as DLLs). Inclui apollocloud/apollocloudcmd nas excecoes.
          $bl = "${{ github.workspace }}\${{ env.CRAFT_TARGET }}\etc\blueprints\locations\craft-blueprints-nextcloud\nextcloud-client\blacklist.txt"
          if (Test-Path $bl) {
            $blc = Get-Content $bl -Raw
            $blc = $blc.Replace('nextcloud|nextcloudcmd|QtWebEngineProcess', 'nextcloud|nextcloudcmd|apollocloud|apollocloudcmd|QtWebEngineProcess')
            Set-Content $bl $blc -Encoding utf8 -NoNewline
            Write-Host "[apollo] blacklist corrigido (preserva apollocloud.exe/apollocloudcmd.exe)"
          } else { Write-Host "[apollo] AVISO: blacklist.txt nao encontrado em $bl"; exit 1 }

      - name: Package installer
        shell: pwsh
        run: |
          function craft() {
              python "${{ env.CRAFT_MASTER_LOCATION }}\CraftMaster.py" --config "${{ env.CRAFT_MASTER_CONFIG }}" --target ${{ env.CRAFT_TARGET }} -c $args
              if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
          craft --package nextcloud-client

      - name: Collect installer
        shell: pwsh
        run: |
          $dest = "${{ github.workspace }}\_dist"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          # O instalador tem dezenas de MB; os utilitarios do Craft sao < 1 MB.
          $inst = Get-ChildItem -Path "${{ github.workspace }}" -Recurse -Include *.exe,*.msi -ErrorAction SilentlyContinue |
            Where-Object { $_.Length -gt 5MB -and $_.FullName -notmatch '\\work\\build\\|\\bin\\' } |
            Sort-Object Length -Descending
          if (-not $inst) {
            Write-Host "Nenhum instalador (> 5MB) encontrado; diagnostico:"
            Get-ChildItem -Path "${{ github.workspace }}" -Recurse -Include *.exe,*.msi -ErrorAction SilentlyContinue |
              Sort-Object Length -Descending | Select-Object -First 10 FullName, Length
            exit 1
          }
          $main = $inst[0]
          $target = Join-Path $dest ("ApolloCloud-Setup-x64" + $main.Extension)
          Copy-Item $main.FullName -Destination $target -Force
          Write-Host ("instalador: " + $main.Name + " (" + [math]::Round($main.Length/1MB,1) + " MB) -> " + (Split-Path $target -Leaf))

      - uses: actions/upload-artifact@v4
        with:
          name: apollo-cloud-windows-installer
          path: ${{ github.workspace }}\_dist\**
          if-no-files-found: warn
